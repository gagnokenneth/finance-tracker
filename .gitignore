# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files. .env.local already matches *.local, but a plain .env would
# otherwise be committable — and it holds the Apps Script URL and client ID.
.env
.env.*
!.env.example

# Local-only docs: guides, specs and plans kept on this machine, never pushed.
docs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Subagent-driven-development scratch (ledger, briefs, reports) — not project output
.superpowers/
.worktrees/
